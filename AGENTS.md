# AGENTS.md

本文件用于约束本仓库中由大模型或自动化编码代理产生的代码与文档。

## 与大模型交互规则

- 与大模型交互时，面向用户的回复内容必须使用中文；编写说明文档、开发文档、变更记录和任务描述时也必须使用中文。
- 当需要引用命令、路径、代码标识符、错误信息或第三方 API 名称时，可以保留英文原文，并用中文解释其含义或处理方式。
- 除非用户明确说明，否则不要直接调用浏览器验证页面效果。

## 运行环境与渲染支持

- 当前组件库仅支持浏览器客户端渲染，不支持 SSR 或 hydration。
- 后续公共组件的设计、实现和测试无需考虑 SSR、服务端首屏样式收集、服务端请求隔离或 hydration；不得为这些能力新增适配、公共 API 或专用测试。
- Node 中的 ESM/CommonJS 模块加载属于发布契约检查，不代表支持服务端组件渲染；客户端多应用隔离、局部主题、Teleport 和真实交互契约继续保留。

## Figma 设计参考

- 当前交互使用的 Figma 文件：[serenova-ui](https://www.figma.com/design/RXjt6R6Hhaz4NJP8fYw533/serenova-ui)。
- Button 组件：`node-id` 为 `3-60`，[打开组件节点](https://www.figma.com/design/RXjt6R6Hhaz4NJP8fYw533/serenova-ui?node-id=3-60)。
- Icon 页面：`node-id` 为 `12-10`，[打开页面节点](https://www.figma.com/design/RXjt6R6Hhaz4NJP8fYw533/serenova-ui?node-id=12-10)。

## 公共组件轻量化无障碍规范

- 本规范仅适用于公共组件，不约束测试 Fixture、Storybook 辅助组件等内部实现。
- 公共组件涉及交互时，必须明确梳理鼠标、键盘和焦点相关行为。
- 组件需要支持键盘操作时，应遵循用户熟悉的按键习惯。
- 鼠标与键盘触发同一操作时，必须复用同一业务处理路径，避免行为分叉。
- 交互状态必须使用含义清晰的命名和派生逻辑表达，避免将复杂判断散落在模板中。
- 不得无条件阻止原生事件或焦点行为；使用 `preventDefault`、`stopPropagation` 等操作时，代码必须明确体现原因和适用条件。
- 键盘行为沿用现有组件测试体系；涉及真实焦点或浏览器键盘事件时，使用 `*.integration.spec.ts`，不新增独立的无障碍测试体系。
- 当前不以完整 WCAG 合规为目标，不强制新增 `aria-*`、`role`、特定 HTML 语义标签、可访问名称、屏幕阅读器适配或自动化扫描工具。
- 不得仅为满足本规范而引入额外依赖或复杂抽象；后续可根据组件成熟度逐步增强。

## 组件测试规范

- 新增或修改公共组件时，必须先分析该组件的 Props 及默认值、Slots、Events、渲染语义、交互状态、边界条件、源码类型和公共导出，并从这些公共契约推导测试范围，不得复制其他组件的固定用例集合代替分析。
- 每个公共组件必须在 `tests/components/<组件领域>/` 中具有 `*.unit.spec.ts` 快速黑盒单元测试，并根据适用性验证公共输入、用户交互、可观察输出、状态变化、源码类型和单组件公共导出。
- 单元测试的输入与断言必须基于公共接口和用户可观察结果；不得通过访问内部状态、调用私有方法或断言非契约实现结构证明组件正确性。
- 公共契约涉及原生表单、真实焦点与键盘、浏览器事件、Teleport、滚动与溢出、响应式布局、元素尺寸、计算样式、多个组件或共享上下文协作时，必须增加由 Vitest Browser Mode 执行的 `*.integration.spec.ts` 真实浏览器集成测试；不涉及这些条件时，不得仅为满足数量要求而增加集成测试。
- 集成测试必须挂载验证契约所需的最小 Fixture；鼠标与键盘触发同一操作时，必须通过用户可观察结果验证两种输入获得一致的业务行为。
- Playwright E2E 必须使用最小消费应用，通过公共 JS 入口加载当前 `dist` 构建产物；不得显式导入组件库 CSS，组件库不提供独立 CSS 入口，并按代表性关键用户流程组织；不得要求每个公共组件单独具有 E2E。
- 新增或修改的流程涉及多个公共组件、全量或按需安装与全局样式、关键表单或浮层操作链路，或者可能造成高影响消费回归时，必须评估并按需增加 `*.e2e.spec.ts`；首阶段仅要求 Chromium，只有出现明确兼容性风险时才扩展其他浏览器。
- `tests/contracts/package/` 中的发布契约测试独立于单元、集成和 E2E 三层，必须继续验证构建产物的 ESM、CommonJS、类型声明、自动样式、插件、导出映射和 npm 文件边界；不得以浏览器 E2E 替代这些检查。
- Storybook 仅用于开发预览、交互调试和组件文档，不作为自动化测试载体或发布质量门槛；Story 的存在和 `play` 函数不得替代组件公共行为测试。
- 公共组件或类型导出发生变化时，必须同步更新源码出口测试和共享包消费 Fixture。
- 单个组件或用例使用的 Fixture 必须与所属测试共置；跨组件复用的 setup、helper 或 Fixture 必须放入 `tests/support/`，E2E 消费应用及其专用资产必须放入 `tests/e2e/`。
- 结构快照投入使用时必须与明确的行为或语义断言配套，不得作为正确性的唯一证明。
- 组件任务完成前必须记录 unit、integration、E2E 和 package 各层是否适用及理由，并运行测试类型检查和所有受影响的 `test:unit`、`test:integration`、`test:e2e`、`test:package` 质量命令，在变更说明中记录验证结果。

## Git 提交信息规范

- 提交信息必须遵循 Conventional Commits，标题格式为 `<type>(<scope>): <subject>`；`scope` 可省略。
- `type` 仅可使用 `build`、`chore`、`ci`、`docs`、`feat`、`fix`、`perf`、`refactor`、`revert`、`style`、`test`。
- `scope` 应使用英文小写的组件名或模块名。
- `subject` 应使用中文简洁描述主要变更，不以句号结尾，禁止使用 `update`、`fix bug`、`修改代码` 等含义不明确的描述。
- 提交正文可省略；需要补充变更点时，标题后必须空一行，且每条非空信息必须以 `- ` 开头。
- 每个正文列表项只描述一个变更点，使用中文且不以句号结尾。
- 破坏性变更必须在 `type` 或 `scope` 后添加 `!`，并在正文后以 `BREAKING CHANGE:` 说明迁移影响。
- 一次提交只包含一个逻辑变更。

示例：

```text
feat(button): 增加按钮加载状态

- 支持 loading 属性
- 加载时阻止重复点击
```

破坏性变更示例：

```text
feat(theme)!: 调整主题配置结构

- 使用令牌对象替代扁平配置
- 移除旧版颜色字段

BREAKING CHANGE: 旧版主题配置需要迁移至新的令牌结构。
```
